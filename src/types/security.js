/**
 * @typedef {object} securityResult security check results
 * @property {missingPermissions} missingPermissions array of missing permissions
 * @property {boolean} pass if user passed security check
 */

/**
 * @typedef {array<string>} missingPermissions array of missing permissions
 */